{
  "name": "babel-plugin-ng-hot-reload-workspace",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "lib",
    "examples/*"
  ],
  "scripts": {
    "postinstall": "yarn --cwd lib build"
  }
}
